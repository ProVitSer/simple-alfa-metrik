export enum RESTUrlPath {
    login = '/auth/login',
    customer = '/customer/',
} 

export enum ApiVersion {
    v2 = "/v2api"
}

export enum ApiMethod {
    index= 'index',
    create = 'create',
    update = 'update',
    delete = 'delete'
}